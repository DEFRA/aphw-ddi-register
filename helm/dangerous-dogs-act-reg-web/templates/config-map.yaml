{{- include "ffc-helm-library.container-config-map" (list . "dangerous-dogs-act-reg-web.config-map") -}}
{{- define "dangerous-dogs-act-reg-web.config-map" -}}
data:
  NODE_ENV: {{ quote .Values.environment }}
  PORT: {{ quote .Values.container.port }}
  APPINSIGHTS_CLOUDROLE: {{ quote .Values.name }}
{{- end -}}
